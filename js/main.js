/*var myModule = (function () {

    var init = function () {
        // сюда пишем то, что должно произойти сразу. Как правило, это функция "обработчики событий"
        _setUpListeners();

    };

    var _setUpListeners = function () {
        $('.header__title').on('click', _goToStart);
    };


    var _goToStart = function(){
        location.href = '/';
    };

    /* Все, что пишется до ретурна, нельзя поменять из консоли */
   /* return {
        init: init
    };

})();

myModule.init();*/